persistence:
  enabled: true
  storageClass: managed-nfs-storage

securityContext:
  
env:
  open: 
    DISABLE_API: false
    ALLOW_OVERWRITE: true

resources:
  requests:
    memory: 128Mi
    cpu: 0.1
  limits:
    memory: 512Mi
    cpu: 0.2
